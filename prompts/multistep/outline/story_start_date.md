Here is the story's core foundation:

<STORY_CONTEXT>
{prompt}
</STORY_CONTEXT>

## Your Task

Provide a suitable story start date based on the story context above.

If no start date is provided in the context, think of a suitable start date based on the
setting and themes. Don't use any placeholders for years, months, days. Just go with the
first real date that you think of, but something sensible (not the 1st January, for example).

## Output Format

Respond with ONLY a single date in `YYYY-MM-DD` format (for example: `2047-06-15`).

Do not wrap the date in tags, quotes, or code fences. Do not provide any commentary,
labels, or metadata. The entire response must be the date and nothing else.