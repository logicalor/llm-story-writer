"""Legacy CLI stub - no longer supported. Use the OpenCode tool suite."""


def main() -> None:
    """Legacy CLI entry point - raises NotImplementedError."""
    raise NotImplementedError(
        "The legacy CLI is no longer supported. "
        "Use the OpenCode tool suite instead: run opencode in the project root."
    )


if __name__ == "__main__":
    main()
